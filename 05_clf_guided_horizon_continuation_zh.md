# CLF 引导的两阶段时域延拓 iLQR：算法、计算与证明路线

> 2026-09-06 研究范围更新：原问题的控制取值为 $u_k\in\mathbb R^m$，不施加输入范围约束。终端集、CLF 进度条件和局部校正邻域属于证书或算法条件，不是原问题的输入限制。本文保留历史路线的职责；当前修订及证明边界以 [06_review_and_revised_method_zh.md](06_review_and_revised_method_zh.md) 为准。

> 2026-09-05 审计更新：本文是候选路线记录。固定时域安全校正、终端集合兼容性及无限时域函数空间等问题的反例与修订，见 [06_review_and_revised_method_zh.md](06_review_and_revised_method_zh.md)。不能将本文尚未闭合的接口当作已证明结论。

## 0. 文档定位

本文整理当前正在形成的新算法路线。核心目标是：不预先假设已知一条进入终端集的可行轨迹，而是在第一阶段通过在线控制 Lyapunov 函数下降问题逐步生成可认证的前缀；进入局部终端集后，再切换到残差强迫的认证延拓阶段。

本文试图同时回答以下问题：

1. 时域从一开始到底求解什么问题；
2. 一步时域延拓如何计算；
3. 一步 CLF 不成立时，有限步 CLF 如何计算；
4. 新生成的轨迹为什么不会破坏到达终端集的进度；
5. iLQR 校正如何与 CLF 扩展结合；
6. 第一阶段何时结束，第二阶段何时开始；
7. 哪些结论来自在线可验证条件，哪些结论仍需要额外假设；
8. 为什么该方法不同于直接选取一个很大的固定时域并运行 iLQR。

本文中的所有轨迹均由原始非线性动力学精确 rollout 产生。线性化模型、冻结 LTV 模型和二次近似只用于生成候选方向，不能单独充当原系统的可行性、下降性或收敛性证书。

需要特别说明：本文描述的是候选的新主线。当前论文和现有实验代码仍包含“已知有限步可达见证”的旧第一阶段，因此在完成同步修改前，不能声称现有代码已经实现本文算法。

---

## 1. 问题设置与四个设计目标

考虑离散时间控制仿射系统

$$
x_{k+1}=F(x_k,u_k)=a(x_k)+B(x_k)u_k,
$$

其中

$$
x_k\in\mathbb R^n,
\qquad
u_k\in\mathbb R^m.
$$

原问题没有输入幅值范围。有限步 CLF 的收缩、中间增长要求和数值信赖域用于生成证书与控制模型误差，不应改写成原问题的输入限制。

无限时域性能指标为

$$
J_\infty(U;x_0)
=
\sum_{k=0}^{\infty}
\ell(x_k,u_k),
$$

其中

$$
\ell(x,u)=x^\top Qx+u^\top Ru,
\qquad
Q\succ0,
\qquad
R\succ0.
$$

研究目标不是直接求一个全局无限时域最优解，而是设计一列增长的有限时域问题，使算法尽可能满足：

1. 每个固定时域上的校正过程具有明确的下降和停止判据；
2. 算法可以持续增加时域，而不是依赖预先指定的最大时域；
3. 固定前缀极限满足无限时域的一阶必要条件，并在二阶条件下具有严格局部最优性；
4. 每个新时域只需要少量校正，从而在计算上优于一次求解一个很大的固定时域问题。

这四个目标对应四种不同的理论义务：

- 固定时域 iLQR 的数值收敛；
- 第一阶段的终端集可达性；
- 第二阶段的无穷延拓与固定前缀收敛；
- 极限点的一阶和二阶最优性质。

它们不能由同一个 Lyapunov 不等式一次性推出。

---

## 2. 三类终端对象及其职责

算法中需要区分三个不同对象。

### 2.1 第一阶段的进度函数

选择

$$
W(x)=x^\top Sx,
\qquad
S\succ0.
$$

第一阶段利用它判断一个新扩展是否取得了朝终端区域前进的可认证进度。它可以由原点线性化后的 DARE 矩阵构造，也可以由其他局部或数据驱动方法构造。

但是，DARE 得到的二次型通常只在平衡点附近具有局部 Lyapunov 性质。不能仅凭 DARE 就假设它在整个初始子水平集上满足统一的一步 CLF 条件。

### 2.2 第一阶段的移动终端代价

在第一阶段的某个固定时域问题中，使用

$$
\Phi_N^{\mathrm I}(x_N)
=
\lambda_NW(x_N),
\qquad
\lambda_N>0.
$$

这里的权重可以根据一步或有限步收缩子问题在线确定。其职责是：

- 使扩展器倾向于产生更小的终端 CLF 值；
- 与收缩约束的 KKT 乘子相匹配；
- 为后续固定时域 iLQR 提供正定终端曲率。

第一阶段的移动终端代价是数值和可达性工具，不应被直接解释为原无限时域问题的精确尾价值函数。

### 2.3 第二阶段的局部终端证书

选择局部终端三元组

$$
\left(
\mathcal X_f,
V_f,
\kappa_f
\right),
$$

其中

$$
\mathcal X_f
=
\left\{
x:W(x)\leq\alpha_f
\right\},
$$

并且对所有

$$
x\in\mathcal X_f
$$

满足

$$
F\left(x,\kappa_f(x)\right)\in\mathcal X_f,
$$

以及

$$
V_f\left(F\left(x,\kappa_f(x)\right)\right)-V_f(x)
\leq
-\ell\left(x,\kappa_f(x)\right).
$$

这个终端三元组保证从终端集中的状态出发，存在一条动态可行且无限代价有限的局部反馈尾部。它本身不保证有限前缀已经驻定，也不保证该前缀局部最优。

### 2.4 一个标准的局部构造

在零平衡点和零平衡输入处，线性化矩阵为

$$
A_0=F_x(0,0)=a_x(0),
$$

$$
B_0=F_u(0,0)=B(0).
$$

若

$$
(A_0,B_0)
$$

可稳定，则离散代数 Riccati 方程为

$$
P
=
Q+A_0^\top PA_0
-A_0^\top PB_0
\left(
R+B_0^\top PB_0
\right)^{-1}
B_0^\top PA_0.
$$

定义

$$
K_f
=
\left(
R+B_0^\top PB_0
\right)^{-1}
B_0^\top PA_0,
$$

$$
\kappa_f(x)=-K_fx.
$$

线性闭环矩阵为

$$
A_c=A_0-B_0K_f,
$$

并满足

$$
A_c^\top PA_c-P
=
-Q-K_f^\top RK_f.
$$

对原非线性闭环，可以写成

$$
F(x,\kappa_f(x))
=
A_cx+r_f(x),
$$

其中在足够光滑条件下

$$
\left\|r_f(x)\right\|
=
O\left(\left\|x\right\|^2\right).
$$

于是可以选取

$$
V_f(x)=\gamma x^\top Px,
\qquad
\gamma>1,
$$

再把终端水平

$$
\alpha_f
$$

取得足够小，使正不变性和真实非线性 Lyapunov 下降同时成立。

第一阶段的进度矩阵可以取

$$
S=P,
$$

但这不意味着该二次型自动成为大区域上的一步 CLF。DARE 构造首先给出的是局部终端性质。

---

## 3. 整体算法的两阶段结构

### 3.1 第一阶段：获得终端安全前缀

第一阶段从时域一开始，不需要任何已知可行轨迹。算法在当前末端状态处执行：

1. 优先尝试一步 CLF 收缩；
2. 一步收缩失败时，尝试有限步 CLF 收缩；
3. 只接受经过原始非线性动力学验证的控制块；
4. 在不破坏 CLF 进度预算的前提下，进行有限次固定时域 iLQR 性能校正；
5. 重复增加物理时域，直到真实末端状态进入局部终端集。

第一阶段只负责获得一条可接上局部稳定尾部的有限前缀。它不负责证明该前缀已经满足最终的驻点精度。

### 3.2 切换校正：在固定时域更换终端代价

第一次得到

$$
x_N\in\mathcal X_f
$$

后，不立即增加时域。先保持时域不变，把第一阶段终端代价

$$
\lambda_NW(x_N)
$$

替换为第二阶段的真实终端代价

$$
V_f(x_N),
$$

然后重新运行固定时域 iLQR，直到精确 KKT 残差满足首次认证阈值，同时所有被接受的 rollout 仍满足

$$
x_N\in\mathcal X_f.
$$

这是从“可达前缀”进入“认证延拓分支”的必要接口。

为给切换校正留出安全裕度，更稳妥的做法是定义严格内部切换集

$$
\mathcal X_{\mathrm{sw}}
=
\left\{
x:W(x)\leq\alpha_{\mathrm{sw}}
\right\},
$$

其中

$$
0<\alpha_{\mathrm{sw}}<\alpha_f.
$$

第一阶段以进入切换集为目标，而局部终端反馈的认证区域仍为较大的终端集。这样，小幅固定时域校正不容易立即越出终端集。若不设置内部裕度，则必须在每个试探 rollout 中显式保持终端成员条件。

### 3.3 第二阶段：残差强迫的认证延拓

第二阶段利用终端反馈延长轨迹，并在每个候选时域上进行残差强迫校正。其目标不再是证明末端可以进入终端集，而是建立：

- 终端尾部始终动态可行；
- 延拓后的驻点残差可由有限次 corrector 降到指定阈值；
- 时域趋于无穷时，每个固定前缀收敛；
- 极限控制满足无限时域一阶必要条件；
- 若再有适当的二阶强制性，则极限解严格局部最优。

---

## 4. 初次迭代到底求解什么

算法开始时只给定初始状态

$$
x_0.
$$

此时可以把已知轨迹记为零时域轨迹，但第一次真正求解的问题必须是时域一问题。因此，所谓“从时域一开始”是指第一次决策变量只有

$$
u_0,
$$

并由原始动力学得到

$$
x_1=a(x_0)+B(x_0)u_0.
$$

首先计算一步最小可达 CLF 值

$$
w_1^{\min}(x_0)
=
\min_{u_0\in\mathbb R^m}
W\left(a(x_0)+B(x_0)u_0\right).
$$

若选定的收缩率满足

$$
w_1^{\min}(x_0)
\leq
\rho_1W(x_0),
\qquad
0<\rho_1<1,
$$

则求解带硬收缩约束的性能问题

$$
\begin{aligned}
\min_{u_0\in\mathbb R^m}
\quad &
x_0^\top Qx_0
+u_0^\top Ru_0
+\lambda_bW(x_1)
\\
\mathrm{s.t.}\quad &
x_1=a(x_0)+B(x_0)u_0,
\\
&W(x_1)\leq\rho_1W(x_0).
\end{aligned}
$$

这个问题的解给出第一条控制和第一个新增状态。若一步收缩不可行，则不应随意选择一步贪心控制，而应进入有限步 CLF 扩展器。

---

## 5. 一步 CLF 扩展的完整计算

### 5.1 一步最小 CLF 问题

固定当前状态

$$
x,
$$

简记

$$
a=a(x),
\qquad
B=B(x).
$$

因为

$$
W(a+Bu)
=
u^\top B^\top SBu
+2a^\top SBu
+a^\top Sa,
$$

所以

$$
\min_{u\in\mathbb R^m}W(a+Bu)
$$

是凸二次规划。当

$$
S\succ0
$$

但

$$
B^\top SB
$$

可能奇异时，目标仍凸但不一定严格凸。令 $C=S^{1/2}B$、$d=S^{1/2}a$，问题等价于最小二乘 $\min_u\|Cu+d\|^2$。全部解及最小值为

$$
u^{\min}=-C^\dagger d+(I-C^\dagger C)z,\quad z\in\mathbb R^m,
\qquad w_1^{\min}(x)=\|(I-CC^\dagger)d\|^2.
$$

这里 $C^\dagger$ 是 Moore--Penrose 伪逆；取 $z=0$ 得到最小范数解。有限维线性像闭，故该最小二乘值可达，即使控制域无界也不需输入紧致性。不能加入任意小正则后把所得 CLF 值当作原第一层的精确最小值；正则会改变最小化目标。

一步乘法收缩条件为

$$
\min_{u\in\mathbb R^m}
W\left(F(x,u)\right)
\leq
\rho_1W(x),
\qquad
0<\rho_1<1.
$$

等价地，令

$$
\eta_1=1-\rho_1,
$$

则

$$
\min_{u\in\mathbb R^m}
W\left(F(x,u)\right)
\leq
\left(1-\eta_1\right)W(x).
$$

### 5.2 带终端权重的一步问题

考虑

$$
\min_{u\in\mathbb R^m}
\left\{
u^\top Ru+
\lambda W(a+Bu)
\right\}.
$$

对本研究的无输入范围问题，一阶条件为

$$
2Ru+2\lambda B^\top S(a+Bu)=0.
$$

因此

$$
u_\lambda
=
-\lambda
\left(
R+\lambda B^\top SB
\right)^{-1}
B^\top Sa.
$$

由于

$$
R\succ0,
$$

矩阵

$$
R+\lambda B^\top SB
$$

对 $\lambda\ge0$ 始终正定。直接解该正定线性方程，无需饱和或投影。

### 5.3 终端权重的单调搜索

设

$$
u_\lambda
\in
\arg\min_{u\in\mathbb R^m}
\left\{
c(u)+\lambda W(F(x,u))
\right\},
$$

其中

$$
c(u)=u^\top Ru.
$$

对

$$
0\leq\lambda_1<\lambda_2,
$$

由两个最优性不等式相加可得

$$
\left(\lambda_2-\lambda_1\right)
\left[
W(F(x,u_{\lambda_2}))
-W(F(x,u_{\lambda_1}))
\right]
\leq0.
$$

因此最优解所达到的终端 CLF 值关于权重单调不增。一步问题是凸问题，所以可以先用倍增搜索确定权重上界，再用二分搜索找到满足收缩条件的近似最小权重。

若只有

$$
w_1^{\min}(x)=\rho_1W(x),
$$

则所需权重可能只在趋于无穷时逼近。因此，若要保证存在有限搜索上界，通常需要严格可行性

$$
w_1^{\min}(x)<\rho_1W(x).
$$

### 5.4 更稳妥的两级问题

实际实现中，更推荐先求收缩可行性，再求性能：

第一层：

$$
w_1^{\min}(x)
=
\min_{u\in\mathbb R^m}W(F(x,u)).
$$

第二层：

$$
\begin{aligned}
\min_{u\in\mathbb R^m}
\quad &
u^\top Ru+\lambda_bW(F(x,u))
\\
\mathrm{s.t.}\quad &
W(F(x,u))\leq\rho_1W(x).
\end{aligned}
$$

若收缩约束的 KKT 乘子为

$$
\nu\geq0,
$$

则拉格朗日函数中的有效终端权重为

$$
\lambda^+=\lambda_b+\nu.
$$

因此下一次固定时域校正可以采用

$$
\Phi^{\mathrm I}(z)=\lambda^+W(z).
$$

这里需要满足相应的约束资格条件，才能合法使用 KKT 乘子解释。

### 5.5 在可行权重中改善 iLQR 边界连续性

CLF 收缩只控制终端状态大小，不保证增加时域后旧前缀的价值梯度和曲率变化很小。可以在已经满足收缩条件的权重中，再选择 Bellman 边界缺陷较小的权重。

设扩展前的终端权重为

$$
\lambda^-,
$$

当前末端状态为

$$
x,
$$

则旧终端局部模型的梯度和 Hessian 为

$$
p^-=2\lambda^-Sx,
$$

$$
P^-=2\lambda^-S.
$$

对某个候选权重

$$
\lambda,
$$

求得一步控制

$$
u_\lambda
$$

和真实后继状态

$$
x^+=F(x,u_\lambda).
$$

新终端局部模型为

$$
p^+=2\lambda Sx^+,
$$

$$
P^+=2\lambda S.
$$

令

$$
A=F_x(x,u_\lambda),
\qquad
B=F_u(x,u_\lambda).
$$

一步 iLQR Bellman 回推给出

$$
\widehat p
=
2Qx+A^\top p^+,
$$

$$
Q_{xx}
=
2Q+A^\top P^+A,
$$

$$
Q_{ux}
=
B^\top P^+A,
$$

其中

$$
Q_{xu}=Q_{ux}^\top.
$$

$$
Q_{uu}
=
2R+B^\top P^+B,
$$

$$
\widehat P
=
Q_{xx}-Q_{xu}Q_{uu}^{-1}Q_{ux}.
$$

这些式子直接对应无输入范围约束的局部控制消元；需要所求逆的控制 Hessian 正定。显式 CLF 收缩约束的乘子仍按该约束的 KKT 系统处理。

定义边界缺陷

$$
d_p(\lambda)=\widehat p-p^-,
$$

$$
D_P(\lambda)=\widehat P-P^-.
$$

可以在满足真实一步收缩的有界权重区间内求解一维问题

$$
\min_{\lambda}
\left\{
\omega_p\left\|d_p(\lambda)\right\|^2
+
\omega_P\left\|D_P(\lambda)\right\|_F^2
+
\omega_\lambda
\left|\lambda-\lambda^-\right|^2
\right\}.
$$

这个计算只能改善 iLQR 意义下的边界连续性，不能保证缺陷精确为零。一个标量终端权重通常没有足够自由度同时匹配完整梯度和 Hessian。

有限步控制块也可以通过沿块进行一次 Riccati 回推来定义类似的块边界缺陷，但其代价高于一步情形。因此，当前建议是把它作为可选诊断或权重选择准则，而不是第一阶段每次扩展都必须求解的核心问题。

### 5.6 为什么不用二次松弛惩罚提取终端权重

若使用

$$
\rho_s s^2
$$

作为松弛变量代价，则在

$$
s=0
$$

处对松弛变量的导数为零。此时活动收缩约束通常无法由一个正乘子与松弛变量的一阶条件同时平衡。因此，不应把二次松弛问题在零松弛处的乘子直接解释为所需的终端权重。

稳妥做法是：

1. 使用字典序的最小松弛问题判断是否能达到零松弛；
2. 达到零松弛后，重新求解硬收缩约束性能问题；
3. 从硬约束问题中读取乘子。

---

## 6. 一步 CLF 假设的强弱

需要区分三种完全不同的表述。

### 6.1 局部一步 CLF

若原点线性化可稳定，DARE 给出的二次型常常可以在足够小的邻域内满足一步下降。这是局部终端设计中的常见条件。

### 6.2 沿实际生成轨迹的一步证书

算法每到一个实际访问状态，只求解一次一步 QP，并检查当前状态是否能收缩。这是在线的后验认证，不要求整个区域中的所有状态都满足一步条件。

如果每次实际接受的扩展都满足

$$
W(x_{k+1})
\leq
\left(1-\eta_k\right)W(x_k),
$$

并且

$$
\sum_{k=0}^{\infty}\eta_k=\infty,
$$

则

$$
\prod_{k=0}^{\infty}
\left(1-\eta_k\right)=0,
$$

从而沿该次运行有

$$
W(x_k)\to0.
$$

这是运行轨迹上的条件性结论，不是区域性稳定定理。

### 6.3 区域一致一步 CLF

若假设对整个第一阶段区域中的每个状态都存在一步控制，使

$$
\min_{u\in\mathbb R^m}W(F(x,u))
\leq
\rho_1W(x),
\qquad
0<\rho_1<1,
$$

则得到了一个无记忆、区域一致、几何收缩的稳定化性质。这通常是很强的假设，特别是在欠驱动系统、具有相对阶的系统或控制作用存在时滞的系统中。

它甚至可能比“只对给定初值存在一条有限可达轨迹”更强。因此，主算法不应把区域一致一步 CLF 作为唯一基础，而应允许有限步下降。

---

## 7. 有限步 CLF 的定义与宏系统解释

### 7.1 固定长度有限步 CLF

给定整数

$$
M\geq2,
$$

从状态

$$
x
$$

出发，若存在控制块

$$
U_M=
\operatorname{col}
\left(
u_0,\ldots,u_{M-1}
\right)
\in\mathbb R^{mM},
$$

使真实轨迹满足

$$
x_{j+1}=F(x_j,u_j),
\qquad
x_0=x,
$$

以及

$$
W(x_M)\leq\rho_MW(x),
\qquad
0<\rho_M<1,
$$

则称该控制块在状态处提供了一个长度为

$$
M
$$

的 CLF 收缩证书。

为了排除危险的中间瞬态，还应要求

$$
W(x_j)\leq c_MW(x),
\qquad
j=1,\ldots,M-1,
$$

其中

$$
c_M\geq1.
$$

如果系统还具有状态约束，则中间状态也必须逐点满足状态约束。

### 7.2 宏系统

定义精确的多步流映射

$$
x_M=F_M(x,U_M).
$$

那么有限步 CLF 条件可以写成

$$
\min_{U_M\in\mathcal Z_M(x)}
W(F_M(x,U_M))
\leq
\rho_MW(x),
$$

其中

$$
\mathcal Z_M(x)
$$

仅包含所选的中间增长条件，以及另行研究状态约束扩展时的状态条件；它不包含输入取值范围。该集合在控制坐标中未必紧。上式的 $\min$ 仅在最小值可达时使用；一般应读作 $\inf$，运行时证书必须由实际控制块实现收缩不等式，不能只凭下确界等于阈值断言证书存在。

这说明有限步 CLF 在数学上就是提升系统

$$
x^+=F_M(x,U_M)
$$

的一步 CLF。困难只在于：即使原系统每一步对控制仿射，多步流映射通常也不再关于整个控制块仿射。

---

## 8. 无已知可行轨迹的有限步两级求解

### 8.1 第一层：最小收缩违反量

在当前末端状态

$$
x_N
$$

处，求解

$$
\begin{aligned}
s_M^{\inf}(x_N)
=
\inf
\quad &s
\\
\mathrm{s.t.}\quad
&x_0=x_N,
\\
&x_{j+1}=a(x_j)+B(x_j)u_j,
\\
&u_j\in\mathbb R^m,
\qquad
j=0,\ldots,M-1,
\\
&W(x_M)\leq\rho_MW(x_N)+s,
\\
&W(x_j)\leq c_MW(x_N)+s,
\qquad
j=1,\ldots,M-1,
\\
&s\geq0.
\end{aligned}
$$

无界控制域上，这个下确界未必可达：$s_M^{\inf}=0$ 本身不等于已找到零松弛点。实现保存实际候选 $(U_M,s_M)$；只有该候选经真实 rollout 验证 $s_M=0$ 后才进入第二层。若要声称第一层最优解存在，需另证强制性或紧的有效子水平集，不能引用不存在的输入紧致性。

这是一个字典序第一层问题，其唯一主要目标是把终端收缩和中间增长条件的共同违反量降到零。也可以为每条不等式设置独立非负松弛，再以松弛量的最大值或加权和作为第一层目标。

如果没有硬状态约束，则可以从任意维数正确、数值有限且 rollout 定义良好的控制块开始，例如全零控制。由原始动力学向前 rollout 即可自动满足所有动力学等式，而终端收缩和中间增长不足由松弛变量承担。因此，发现过程不要求预先给定一条满足收缩条件的轨迹。

若有硬状态约束，可以在发现阶段给状态约束也加入单独松弛，但最终只接受所有松弛均为零的轨迹。

### 8.2 第二层：在硬收缩下优化性能

若第一层找到了

$$
s_M=0,
$$

则使用该控制块作为初值，求解

$$
\begin{aligned}
\min
\quad &
\sum_{j=0}^{M-1}
\left(
x_j^\top Qx_j+u_j^\top Ru_j
\right)
+\lambda_bW(x_M)
\\
\mathrm{s.t.}\quad
&x_0=x_N,
\\
&x_{j+1}=a(x_j)+B(x_j)u_j,
\\
&u_j\in\mathbb R^m,
\\
&W(x_M)\leq\rho_MW(x_N),
\\
&W(x_j)\leq c_MW(x_N).
\end{aligned}
$$

第二层包含 $R\succ0$ 的控制能量项，因而当 $\lambda_b\ge0$ 时目标至少为 $\lambda_{\min}(R)\|U_M\|^2$。若有限步流映射在整个控制空间连续、硬收缩可行集非空且闭，则连续目标的非空子水平集紧，从而最优解存在。这里的紧性来自目标强制性，不是输入幅值上界；仍不能据此保证局部 SQP 求得全局最优解。

若终端收缩约束对应的 KKT 乘子为

$$
\nu_N\geq0,
$$

则有效终端权重为

$$
\lambda_{N+M}=\lambda_b+\nu_N.
$$

后续固定时域 iLQR 可以冻结使用

$$
\Phi_{N+M}^{\mathrm I}(z)
=
\lambda_{N+M}W(z).
$$

该 KKT 解释要求局部求解结果满足一阶必要条件，并满足适当的约束资格条件。若这些条件未验证，则终端权重只能视为数值选取，不能称为由乘子严格导出。

还应注意，这个乘子等价性只针对块起点固定时的有限步子问题。它不说明此前整个控制前缀已经对移动终端代价驻定。把控制块接入完整前缀后，仍需由固定时域 iLQR 和精确 KKT 残差完成全前缀校正。

### 8.3 求解器失败的正确解释

有限步问题一般非凸。局部求解器找到零松弛点，足以给出一条有效的运行时证书；但局部求解器停在正松弛点，只能说明当前初始化和当前求解器未找到证书，不能说明有限步可行控制块不存在。

因此算法状态应区分：

- 已找到并验证有限步证书；
- 当前局部求解失败；
- 达到最大尝试步长但未找到证书；
- 经过全局认证证明不可行。

最后一种状态只有使用完备全局方法时才能给出，普通 SQP 或 iLQR 不能给出。

---

## 9. 有限步问题的高效计算

### 9.1 精确多重射击形式

把

$$
x_1,\ldots,x_M,
\qquad
u_0,\ldots,u_{M-1}
$$

都作为变量，并显式保留动力学等式。这种多重射击形式保留了块带状稀疏结构，适合使用稀疏 SQP 或 Riccati 型线性代数。

直接把所有状态消去虽然减少了变量数，但会形成较稠密的控制块 Hessian；当维数增加时，多重射击通常更适合与 iLQR 代码复用。

### 9.2 沿名义控制块线性化

设名义控制块和精确 rollout 为

$$
\bar U_M=
\operatorname{col}
\left(
\bar u_0,\ldots,\bar u_{M-1}
\right),
$$

$$
\bar x_{j+1}=F(\bar x_j,\bar u_j).
$$

控制仿射系统的 Jacobian 为

$$
A_j
=
F_x(\bar x_j,\bar u_j)
=
a_x(\bar x_j)
+
\sum_{r=1}^{m}
\bar u_{j,r}
\frac{\partial B_{\cdot r}}{\partial x}(\bar x_j),
$$

$$
B_j
=
F_u(\bar x_j,\bar u_j)
=
B(\bar x_j).
$$

一阶扰动动力学为

$$
\delta x_{j+1}
=
A_j\delta x_j+B_j\delta u_j,
\qquad
\delta x_0=0.
$$

消去中间状态后，终端扰动满足

$$
\delta x_M=\mathcal C_M\delta U_M,
$$

其中

$$
\mathcal C_M
=
\begin{bmatrix}
A_{M-1}\cdots A_1B_0
&
A_{M-1}\cdots A_2B_1
&
\cdots
&
B_{M-1}
\end{bmatrix}.
$$

因此终端状态的一阶预测为

$$
x_M
\approx
\bar x_M+\mathcal C_M\delta U_M.
$$

若把第

$$
j
$$

个控制到终端状态的敏感度记为

$$
G_j
=
A_{M-1}\cdots A_{j+1}B_j,
$$

则终端 CLF 对该控制的梯度为

$$
\nabla_{u_j}W(x_M)
=
2G_j^\top Sx_M.
$$

相应的 Gauss--Newton 型终端 Hessian 为

$$
2\mathcal C_M^\top S\mathcal C_M
\succeq0.
$$

这正是有限步逐次凸化子问题保持凸二次结构的原因。

### 9.3 凸 QCQP 子问题

因为

$$
S\succ0,
$$

预测终端约束

$$
\left(
\bar x_M+\mathcal C_M\delta U_M
\right)^\top
S
\left(
\bar x_M+\mathcal C_M\delta U_M
\right)
\leq
\rho_MW(x_N)+s
$$

关于控制增量是凸二次约束。

配合半正定二次阶段代价近似及数值信赖域

$$
\left\|\delta U_M\right\|\leq\Delta,
$$

每个逐次凸化子问题可以写成凸 QCQP。求得候选增量后，必须重新通过原始非线性动力学 rollout。

接受条件至少包括：

$$
W(x_{N+M}^{\mathrm{trial}})
\leq
\rho_MW(x_N),
$$

$$
W(x_{N+j}^{\mathrm{trial}})
\leq
c_MW(x_N),
$$

以及所有真实状态约束成立。

预测模型满足收缩但真实 rollout 不满足时，应缩短信赖域或执行线搜索，而不能接受该候选。

### 9.4 线性或冻结 LTV 情形的显式结构

若短控制块采用仿射 LTV 模型，则终端状态可以写成

$$
x_M=d_M+\mathcal C_MU_M.
$$

令

$$
\bar R_M
=
\operatorname{diag}
\left(
R,\ldots,R
\right).
$$

无约束加权问题

$$
\min_{U_M}
\left\{
U_M^\top\bar R_MU_M
+
\lambda
\left(d_M+\mathcal C_MU_M\right)^\top
S
\left(d_M+\mathcal C_MU_M\right)
\right\}
$$

具有显式解

$$
U_{M,\lambda}
=
-\lambda
\left(
\bar R_M
+\lambda\mathcal C_M^\top S\mathcal C_M
\right)^{-1}
\mathcal C_M^\top Sd_M.
$$

无输入范围约束时先按上式直接求解；加入终端收缩条件后，它是凸 QCQP。

冻结 LTV 解适合作为非线性有限步问题的快速预测器和热启动，但冻结模型上的收缩不能替代真实动力学验证。

### 9.5 如何选择有限步长度

优先尝试较短步长。可选集合例如

$$
\mathcal M
=
\left\{
1,2,4,8,\ldots,M_{\max}
\right\}.
$$

几何候选减少失败尝试次数。若更关心找到最短可行步长，也可以依次尝试

$$
1,2,3,\ldots,M_{\max}.
$$

如果理论假设只说明在所有不超过

$$
M_{\max}
$$

的整数中存在某个可行步长，则算法必须逐一测试所有整数，或者使用能够同时搜索步长的混合优化方法。几何候选可能跳过唯一可行的步长；要使用几何候选给出完备性结论，就必须额外假设可行步长属于预设候选集合。

对于相对阶明显或欠驱动系统，可以参考局部块敏感度矩阵

$$
\mathcal C_M
$$

的秩和条件数选择起始步长。需要强调：满秩不是有限步下降的必要条件，但严重退化的敏感度通常意味着局部凸子问题难以产生有效下降方向。

---

## 10. 可变步有限步 CLF 与逐时刻延拓

### 10.1 可变步条件

固定一个最大块长

$$
M_{\max}.
$$

可变步有限步条件要求：对第一阶段访问到的每个非终端状态，存在某个

$$
M(x)\in
\left\{
1,\ldots,M_{\max}
\right\},
$$

以及一个可行控制块，使

$$
W(x_{M(x)})
\leq
\bar\rho W(x),
\qquad
0<\bar\rho<1.
$$

这比要求每个状态都一步下降弱，也通常比强制恰好在同一个固定步长下降更灵活。

### 10.2 整块提交

最简单且最容易证明的执行方式是：

1. 在当前块起点计算完整控制块；
2. 用真实动力学验证整个控制块；
3. 一次性把整块加入当前优化轨迹；
4. 到块终点后重新求解下一块。

虽然计算上一次加入多个控制，但轨迹数据仍然具有所有中间时刻，因此物理时域仍然经过

$$
N+1,N+2,\ldots,N+M.
$$

### 10.3 逐步提交但保留承诺

如果实现上必须让时域每次只增加一，则可以把已认证控制块放入队列，每次只追加一个控制，但在整块执行完之前不丢弃剩余控制。

也可以在每个中间时刻重新优化剩余控制，但必须保持原块终点和原收缩基准不变。假设块从

$$
x_N
$$

开始，则中间重优化仍必须保证

$$
W(x_{N+M})
\leq
\rho_MW(x_N).
$$

不能每执行一步就重新从当前状态启动一个新的完整长度问题，并把下降截止时刻向后移动。否则有限步下降可能被无限期推迟。

---

## 11. 第一阶段的进度预算

设第

$$
b
$$

个已接受控制块的起点为

$$
N_b,
$$

长度为

$$
M_b,
$$

并满足

$$
W(x_{N_b+M_b})
\leq
\rho_bW(x_{N_b}).
$$

定义

$$
N_{b+1}=N_b+M_b.
$$

同时定义预算递推

$$
\beta_0=W(x_0),
$$

$$
\beta_{b+1}=\rho_b\beta_b.
$$

每次扩展和随后的固定时域 iLQR 校正，都必须保持

$$
W(x_{N_{b+1}})\leq\beta_{b+1}.
$$

因此，iLQR 的性能改进不能用牺牲第一阶段已经获得的可达进度作为代价。

对于 iLQR 的某个试探步，只有同时满足以下条件才接受：

1. 原始有限时域目标真实下降；
2. 精确非线性 rollout 满足所选进度、局部证书条件及另行给定的状态条件；
3. 块终点或当前总终点继续满足进度预算。

如果没有任何步长同时满足这些条件，则保留扩展器给出的原可行轨迹，并报告本次 corrector 未产生可接受更新。这样不会因为性能校正失败而失去 CLF 证书。

---

## 12. 固定时域 iLQR 的计算接口

### 12.1 固定时域问题

在第一阶段的当前总时域

$$
N
$$

上，固定终端权重并求解

$$
J_N^{\mathrm I}(U_N)
=
\sum_{k=0}^{N-1}
\left(
x_k^\top Qx_k+u_k^\top Ru_k
\right)
+\lambda_NW(x_N).
$$

在第二阶段则求解

$$
J_N^{\mathrm{II}}(U_N)
=
\sum_{k=0}^{N-1}
\left(
x_k^\top Qx_k+u_k^\top Ru_k
\right)
+V_f(x_N).
$$

一次固定时域 corrector 内，时域、终端函数和终端权重必须保持不变。

### 12.2 Jacobian

在名义轨迹处

$$
A_k=F_x(x_k,u_k),
\qquad
B_k=F_u(x_k,u_k)=B(x_k).
$$

对控制仿射系统，必须保留

$$
A_k
=
a_x(x_k)
+
\sum_{r=1}^{m}
u_{k,r}
\frac{\partial B_{\cdot r}}{\partial x}(x_k).
$$

只有在线性化点控制为零时，上式第二项才消失。

### 12.3 iLQR 反向递推

设下游局部价值近似为

$$
V_{k+1}(\delta x)
\approx
V_{k+1}^0
+p_{k+1}^\top\delta x
+\frac12\delta x^\top P_{k+1}\delta x.
$$

阶段代价导数为

$$
\ell_x=2Qx_k,
\qquad
\ell_u=2Ru_k,
$$

$$
\ell_{xx}=2Q,
\qquad
\ell_{uu}=2R,
\qquad
\ell_{ux}=0.
$$

iLQR 忽略动力学二阶导数后，局部 Q 函数系数为

$$
q_x
=
\ell_x+A_k^\top p_{k+1},
$$

$$
q_u
=
\ell_u+B_k^\top p_{k+1},
$$

$$
Q_{xx}
=
\ell_{xx}+A_k^\top P_{k+1}A_k,
$$

$$
Q_{ux}
=
\ell_{ux}+B_k^\top P_{k+1}A_k,
$$

$$
Q_{xu}=Q_{ux}^\top,
$$

$$
Q_{uu}
=
\ell_{uu}+B_k^\top P_{k+1}B_k.
$$

加入正则化

$$
\widetilde Q_{uu}=Q_{uu}+\mu I,
\qquad
\mu\geq0.
$$

控制 Hessian 正定时，无约束前馈项和反馈增益为

$$
d_k
=
-\widetilde Q_{uu}^{-1}q_u,
$$

$$
K_k
=
-\widetilde Q_{uu}^{-1}Q_{ux}.
$$

价值递推为

$$
p_k
=
q_x
+K_k^\top\widetilde Q_{uu}d_k
+K_k^\top q_u
+Q_{xu}d_k,
$$

$$
P_k
=
Q_{xx}
+K_k^\top\widetilde Q_{uu}K_k
+K_k^\top Q_{ux}
+Q_{xu}K_k.
$$

在精确求解无约束局部二次问题时，可化简为

$$
p_k
=
q_x-Q_{xu}\widetilde Q_{uu}^{-1}q_u,
$$

$$
P_k
=
Q_{xx}
-Q_{xu}\widetilde Q_{uu}^{-1}Q_{ux}.
$$

数值正则仅服务于方向构造；最终驻点诊断使用未加入该正则项的原目标梯度。

### 12.4 真实前向 rollout

对候选步长

$$
\alpha\in(0,1],
$$

采用

$$
u_k^{\mathrm{trial}}
=
u_k+\alpha d_k
+K_k
\left(
x_k^{\mathrm{trial}}-x_k
\right),
$$

并由

$$
x_{k+1}^{\mathrm{trial}}
=
F
\left(
x_k^{\mathrm{trial}},
u_k^{\mathrm{trial}}
\right)
$$

计算真实轨迹。

第一阶段的接受判据还必须加入进度预算；第二阶段的接受判据还必须保持终端成员关系和相应的信赖域条件。

---

## 13. 精确驻点诊断

iLQR 反向步中的预条件前馈量适合用于生成方向，但最终驻点判断应使用原有限时域问题的精确伴随梯度。

对固定时域问题，伴随变量满足

$$
p_N=\nabla\Phi_N(x_N),
$$

$$
p_k
=
2Qx_k+A_k^\top p_{k+1}.
$$

控制梯度为

$$
g_k
=
2Ru_k+B_k^\top p_{k+1}.
$$

无输入范围约束时，直接令

$$
r_k=g_k.
$$

总残差可取

$$
g_N^{\mathrm{KKT}}
=
\left(
\sum_{k=0}^{N-1}
\left\|r_k\right\|^2
\right)^{1/2}.
$$

当且仅当该残差足够小时，才把当前有限时域轨迹称为近似驻点。末端进入终端集、CLF 值下降或代价下降，都不能替代该驻点检查。

上面的真实梯度残差对应不含额外硬终端条件的原有限时域目标。若把第一阶段进度预算或终端集成员条件作为显式优化约束，并且该约束在解处活动，则完整 KKT 系统还必须包含相应的终端约束乘子。第一阶段并不需要宣称最终驻点，因此可以把进度条件只作为安全接受门；进入第二阶段时则应采用以下两种做法之一：

1. 另证局部驻点与整个校正邻域均严格位于终端集内部，使终端条件不活动；仅有初始点严格在内部不足以保证这一点；
2. 把终端成员约束显式加入有限时域问题，并在驻点诊断中包含其乘子。

---

## 14. 第一步、任意一步和阶段切换的统一算法

### 14.1 初始化

给定：

- 初始状态；
- 原始非线性动力学；
- 无输入范围限制的控制变量 $u_k\in\mathbb R^m$；
- 阶段代价矩阵；
- 第一阶段进度函数；
- 终端三元组；
- 候选有限步长度集合；
- 收缩率、中间增长界和终端阈值；
- iLQR 的正则化、线搜索和残差参数。

初始化为

$$
N=0,
\qquad
x_0\ \text{已知},
\qquad
\beta_0=W(x_0).
$$

第一次调用扩展器时，首先尝试长度一，因此第一个实际优化问题就是时域一问题。

### 14.2 第一阶段的任意一次外层迭代

设当前已有真实轨迹

$$
\left(
x_0,u_0,x_1,\ldots,u_{N-1},x_N
\right).
$$

执行以下步骤：

1. 如果当前末端已经进入终端集，则转入固定时域切换校正。
2. 先尝试预定的一步收缩率；若失败，可在正下界以上回退一步下降量，并记录实际采用的下降量。
3. 一步仍不能取得正下降时，从最短的多步候选块长开始，求最小收缩违反问题。
4. 若某个块长得到零松弛，则求相应的硬收缩性能问题。
5. 用原始非线性动力学重新 rollout 整个控制块。
6. 只有真实终端收缩、中间增长界和所有约束均成立时，才接受该控制块。
7. 更新物理时域、真实轨迹、终端权重和进度预算。
8. 若到达预定校正检查点，冻结当前时域和终端权重，执行有限次 iLQR 校正。
9. 每个 iLQR 试探步都必须保持当前终端进度预算。
10. 返回步骤一。

### 14.3 第一阶段伪代码

~~~text
输入初始状态、动力学、阶段代价、进度函数、终端三元组和参数

令时域为零，当前轨迹只含初始状态
令当前进度预算等于初始进度函数值

循环：
    若真实末端状态位于终端集：
        保持当前时域不变
        把移动终端代价换成认证终端代价
        执行固定时域切换校正
        若终端成员关系和残差门同时成立：
            进入第二阶段
        否则：
            保留最后一条终端安全轨迹并报告切换校正失败

    从最短候选块长开始：
        求最小收缩违反问题
        若未找到零松弛点：
            尝试下一候选块长
        否则：
            求硬收缩性能问题
            在原始非线性动力学上验证完整控制块
            若验证成功：
                接受该控制块
                更新时域、轨迹、终端权重和进度预算
                退出块长搜索

    若所有候选块长均未通过验证：
        返回“有限步延拓证书未找到”

    若当前时域是校正检查点：
        冻结时域和终端权重
        执行固定时域 iLQR
        只接受同时降低目标且保持进度预算的真实 rollout
~~~

这里的文本代码块使用围栏显示，不依赖行内代码格式。

---

## 15. 第一阶段有限命中的证明

### 15.1 固定块长一致收缩

假设所有被接受控制块的长度不超过

$$
M_{\max},
$$

且存在统一常数

$$
0<\bar\rho<1
$$

使每个块都满足

$$
W(x_{N_{b+1}})
\leq
\bar\rho W(x_{N_b}).
$$

递推得到

$$
W(x_{N_b})
\leq
\bar\rho^bW(x_0).
$$

因为终端集阈值满足

$$
\alpha_f>0,
$$

只要

$$
\bar\rho^bW(x_0)
\leq
\alpha_f,
$$

就有

$$
x_{N_b}\in\mathcal X_f.
$$

因此所需控制块数量满足

$$
b_{\mathrm{hit}}
\leq
\left\lceil
\frac{
\log\left(\alpha_f/W(x_0)\right)
}{
\log\bar\rho
}
\right\rceil.
$$

物理时域满足

$$
N_{\mathrm{hit}}
\leq
M_{\max}b_{\mathrm{hit}}.
$$

这里两个对数均为负，因此比值为正。

若采用严格内部切换集，则上述命中证明中的阈值应把

$$
\alpha_f
$$

替换为

$$
\alpha_{\mathrm{sw}}.
$$

### 15.2 变化收缩率

若第

$$
b
$$

块满足

$$
W(x_{N_{b+1}})
\leq
\left(1-\eta_b\right)W(x_{N_b}),
$$

则

$$
W(x_{N_b})
\leq
W(x_0)
\prod_{i=0}^{b-1}
\left(1-\eta_i\right).
$$

若

$$
0<\eta_i<1
$$

且

$$
\sum_{i=0}^{\infty}\eta_i=\infty,
$$

则乘积趋于零。因此对任意正阈值终端集，实际接受块序列最终进入终端集。

该结论是关于被接受序列的。如果算法在某个状态无法找到下一块，则仍然可能停机。要预先排除停机，需要额外的区域有限步 CLF 存在性和求解器成功性条件。

### 15.3 iLQR 校正不破坏证明

每次扩展后，预算已更新为

$$
\beta_{b+1}.
$$

由于 iLQR 只接受满足

$$
W(x_{N_{b+1}}^{\mathrm{trial}})
\leq
\beta_{b+1}
$$

的试探轨迹，所以校正后仍有

$$
W(x_{N_{b+1}})
\leq
\beta_{b+1}.
$$

归纳可知，所有已获得的块级收缩预算均被保留。因此固定时域性能校正不会破坏第一阶段有限命中的证明。

---

## 16. 第一阶段需要怎样的假设

### 16.1 基础常见条件

下列条件较为基础：

1. 动力学连续，并在所访问紧集上具有连续一阶导数；
2. 控制变量取值于 $\mathbb R^m$；有限步全局最小值的存在性需另外证明，不能从输入紧致性推出；
3. 阶段代价矩阵和进度函数矩阵正定；
4. 原始非线性 rollout 可以可靠计算；
5. 局部终端三元组在终端集内满足可行性、不变性和 Lyapunov 下降。

### 16.2 为保证算法永不阻塞所需的较强条件

如果希望得到先验定理，说明算法从整个第一阶段区域中的任意状态都能继续，则需要假设：存在统一最大块长和统一收缩率，使每个非终端状态都存在某个不超过最大块长的可行控制块，并满足中间增长界和终端收缩。

这个区域一致有限步 CLF 条件仍然是实质性假设，但通常弱于区域一致一步 CLF。它要求存在实际的有限控制块；无界控制域上的最小松弛下确界为零不能替代这一存在性要求。

此外，有限步控制块的存在不自动保证局部优化算法能够找到它。若要把存在性直接转化为算法必然成功，还需要以下二者之一：

- 使用具有完备性的全局求解方法；
- 假设初始化始终落在所需局部解的吸引域，并且局部求解器具有相应的收敛性质。

这两个计算条件都不应被隐藏在 CLF 假设中。

### 16.3 更弱的运行时定理

更现实的主定理可以写成：只要算法持续找到经过真实 rollout 验证的控制块，并且实际收缩因子的乘积趋于零，则第一阶段有限进入任意正阈值终端集。

该表述不声称算法能够从所有状态找到控制块，但它对每次实际运行给出可检查证书。区域一致有限步 CLF 与全局求解条件可以作为更强的推论条件单独陈述。

---

## 17. 为什么不使用永久 Bellman 桥接项

为了让时域增加前后的终端梯度和 Hessian 完全一致，可以人为加入一个二次桥接项，使旧终端局部模型与新增一步的 Bellman 回推局部模型精确匹配。

这种构造在代数上可以消除局部边界缺陷，但若每次扩展都永久保留桥接项，会出现三个问题：

1. 人工状态代价随时域不断累积；
2. 求解目标逐渐偏离原始阶段代价和真实终端代价；
3. 少量 iLQR 迭代可能只是因为目标被人为构造成让当前轨迹接近驻点，而不是因为原问题具有良好的延拓正则性。

若之后再消除这些桥接项，又需要额外的同伦过程及新的强正则性假设。因此，当前路线不把永久 Bellman 桥接项作为主算法的一部分。

更合适的用途是把边界梯度缺陷和曲率缺陷作为诊断量，或者在多个已经满足 CLF 收缩的终端权重中选择缺陷较小者。

---

## 18. 从第一阶段切换到第二阶段

### 18.1 进入终端集不等于已经驻定

第一阶段首次得到

$$
x_{N_{\mathrm{hit}}}\in\mathcal X_f
$$

时，只能说明可以从该末端接上局部可行尾部。第一阶段的移动终端代价与第二阶段终端代价不同，因此当前控制前缀不一定是第二阶段有限时域问题的近似驻点。

### 18.2 固定时域切换校正

保持

$$
N=N_{\mathrm{hit}}
$$

不变，替换终端代价并求解

$$
J_N^{\mathrm{II}}(U_N)
=
\sum_{k=0}^{N-1}
\ell(x_k,u_k)
+V_f(x_N).
$$

每个试探 rollout 必须继续满足

$$
x_N\in\mathcal X_f.
$$

只有当

$$
g_N^{\mathrm{KKT}}\leq\tau_N
$$

并且末端仍位于终端集时，才定义首次认证时域

$$
N_{\mathrm{cert}}=N.
$$

一般有

$$
N_{\mathrm{cert}}\geq N_{\mathrm{hit}},
$$

但在当前固定时域切换设计中，如果校正在同一时域成功，则二者数值相同，只是逻辑含义不同。

---

## 19. 第二阶段的认证延拓接口

在已认证轨迹末端使用终端反馈生成候选尾部：

$$
u_k=\kappa_f(x_k),
$$

$$
x_{k+1}=F(x_k,\kappa_f(x_k)).
$$

终端不变性保证候选尾部保持在终端集内，终端 Lyapunov 不等式保证候选无限策略动态可行且尾代价有限。

但是，终端反馈延拓后的完整有限时域控制序列一般不自动满足新的驻点阈值。因此对候选时域

$$
M>N
$$

还需要检查扩展后的精确残差，并运行固定时域 corrector。

典型强迫阈值可取

$$
\tau_N
=
\frac{\tau_0}{(N+1)^p},
\qquad
p>0.
$$

只有当

$$
g_M^{\mathrm{KKT}}\leq\tau_M
$$

时，候选时域才被认证接受。

为了证明每个新时域只需一致有界的校正次数，还需要单独假设或验证：

- 延拓残差相对于新阈值一致有界；
- 固定时域 corrector 在相关局部邻域内具有时域一致的残差收缩率；
- corrector 保持终端集成员关系和所选局部分支。

这些性质不由终端 Lyapunov 下降自动推出。

---

## 20. 为什么不直接选一个很大的固定时域

### 20.1 大固定时域的问题

直接选择很大的时域，需要从一开始同时优化大量控制变量。远端轨迹通常缺乏可靠初值，非线性 rollout、反向递推和线搜索都在整个长时域上进行，局部优化器更容易落入不希望的分支。

此外，若最终只发现所选时域仍然不够，就需要重新扩大问题；而初始大时域本身也不能证明无限时域极限存在。

### 20.2 逐时刻校正也可能是二次复杂度

如果时域每增加一就对整个前缀执行一次完整 iLQR sweep，则总工作量近似为

$$
\sum_{N=1}^{N_{\mathrm{hit}}}O(N)
=
O\left(N_{\mathrm{hit}}^2\right).
$$

因此，“从一开始增长”本身并不能解释计算优势。

### 20.3 当前推荐的计算结构

第一阶段每个新增物理时刻只需要短块 QP、QCQP 或局部 SQP。完整前缀 iLQR 只在几何检查点、残差明显恶化时或首次进入终端集后运行。

若检查点近似满足

$$
N_{j+1}\approx rN_j,
\qquad
r>1,
$$

并且每个检查点只需一致有界的 sweep 数，则总前缀 sweep 工作量满足几何和

$$
O(N_{\max}).
$$

因此，真正的计算论证来自以下组合：

- 短块扩展器只处理新增的少量变量；
- 真实 CLF 证书保护可达进度；
- 完整 iLQR 不在每个整数时域执行；
- 延拓正则性使每个认证检查点只需少量 corrector。

一致有界 corrector 次数仍然需要理论假设或数值证据，不能仅由热启动直觉宣布成立。

---

## 21. 当前可以严格证明的结论

在不假设全局求解器的情况下，只要每个被接受控制块都经过真实动力学验证，就可以严格证明：

1. 所有已接受轨迹满足原始动力学和显式约束；
2. 每个块终点满足记录的 CLF 收缩不等式；
3. 只要收缩因子的乘积趋于零，末端 CLF 值沿块端点趋于零；
4. 对正阈值终端集，持续成功的扩展在有限块数后进入终端集；
5. 进入终端集后，终端反馈给出可行且有限代价的无限尾部；
6. 只要 iLQR 接受规则包含进度预算，性能校正不会破坏已经获得的 CLF 收缩证书。

这些结论不需要假设第一阶段轨迹已经驻定，也不需要假设 iLQR 全局收敛。

---

## 22. 仍需额外假设才能证明的结论

下列结论不能只靠 CLF 收缩得到：

### 22.1 第一阶段对整个区域永不阻塞

需要区域一致的可变步有限步 CLF 存在性，以及能够找到相应控制块的求解器条件。

### 22.2 每个固定时域 corrector 有限完成

需要局部光滑性、正则化 Hessian 的适定性、充分下降线搜索，以及从当前热启动进入相关局部收敛域。

### 22.3 每个新时域只需一致有界的 corrector 次数

需要延拓残差与强迫阈值之间的一致关系，以及时域一致的局部收缩率。

### 22.4 固定前缀收敛

需要选定局部分支的一致性、有限分支误差趋零、终端边界影响在固定前缀上衰减，以及残差到轨迹误差的统一控制。

### 22.5 无限时域一阶驻点

需要有限前缀梯度与无限控制空间梯度的一致性，并能把有限维真实梯度条件传递到无限维极限。

### 22.6 无限时域严格局部最优

除一阶驻点外，还需要无限时域目标在选定加权序列空间中二阶可微，并且 Hessian 在可行切锥上具有一致强制性。

这些条件应分散放置在相应定理之前，而不是集中堆在算法开始处。

---

## 23. 结论边界表

| 结论 | 直接依据 | 不能推出的更强结论 |
|---|---|---|
| 一步或有限步真实收缩 | 精确 rollout 后的 CLF 不等式 | 区域内所有状态都可收缩 |
| 第一阶段运行时进度 | 已接受块的收缩乘积 | 局部求解器永不失败 |
| 有限进入正阈值终端集 | 统一收缩或发散的累计下降 | 有限时间精确到达原点 |
| 终端反馈尾部可行 | 终端不变性和 Lyapunov 下降 | 有限前缀驻定或最优 |
| 固定时域近似驻点 | 精确伴随梯度残差足够小 | 无限时域驻点 |
| 固定前缀收敛 | 分支一致性和边界影响衰减 | 全轨迹一致收敛 |
| 无限时域严格局部最优 | 无限 KKT 加切向二阶强制性 | 全局最优 |
| 少量 corrector | 一致延拓正则性和局部收缩 | 仅由终端 CLF 自动保证 |

---

## 24. 当前最推荐的主算法表述

当前最稳妥的论文主线可以概括为：

1. 用局部终端三元组定义最终认证区域，但不假设已知到达该区域的轨迹；
2. 第一阶段使用在线 CLF 扩展器生成真实可行前缀；
3. 扩展器先尝试一步解析最小二乘与正定线性求解，一步失败时使用可变长度有限步多重射击 SQP；
4. 有限步发现采用最小松弛与硬约束性能优化两个层次；
5. 所有候选只在真实非线性 rollout 通过后接受；
6. 通过进度预算阻止 iLQR 校正破坏终端可达进度；
7. 第一次进入终端集后，在固定时域把移动终端代价替换为认证终端代价并重新校正；
8. 通过终端成员条件和精确 KKT 残差门定义首次认证；
9. 第二阶段使用终端反馈预测、残差强迫 corrector 和几何时域候选持续延拓；
10. 分别陈述有限命中、尾部可行、固定前缀收敛、无限 KKT 和严格局部最优所需条件。

这一路线最大的优点不是消除了所有假设，而是把每个假设的职责隔离开：

- CLF 条件负责第一阶段的可达进度；
- 终端三元组负责安全可行尾部；
- KKT 残差负责驻点认证；
- 延拓正则性负责少量 corrector；
- 分支一致性和边界衰减负责固定前缀极限；
- 二阶强制性负责严格局部最优。

由此可以避免用一个很强但含义模糊的假设同时覆盖所有困难。

---

## 25. 实现前还需作出的具体选择

为了把本文路线落实为代码和论文中的唯一算法版本，还需要固定以下参数和实现选择：

1. 第一阶段进度矩阵的构造方法；
2. 一步和有限步收缩率的取值或回退规则；
3. 中间增长界的取值；
4. 最大有限步长度及候选集合；
5. 有限步子问题采用多重射击 SQP、逐次凸化还是直接非线性规划；
6. 最小松弛问题的停止容差；
7. 真实 rollout 的收缩和约束容差；
8. iLQR 完整前缀校正检查点；
9. 第一阶段移动终端权重的上下界；
10. 切换校正和第二阶段的精确残差阈值。

这些是算法参数，不应被隐藏成理论假设。理论假设应只覆盖存在性、正则性和收敛性，实际参数则必须在算法和实验部分明确报告。

---

## 26. 相关方法的文献定位

有限步 CLF、整块执行、缩短时域重优化以及基于有限步 CLF 的 MPC，可参考：

- [Control of Discrete-Time Nonlinear Systems via Finite-Step Control Lyapunov Functions](https://arxiv.org/abs/1908.09660)

允许在一个有限窗口内选择实际执行步数的 flexible-step 思路，可参考：

- [Flexible-Step Model Predictive Control Based on Generalized Lyapunov Functions](https://arxiv.org/abs/2211.02780)

多重射击、逐次凸化、信赖域和真实非线性轨迹验证的计算框架，可参考：

- [Successive Convexification for Nonlinear Model Predictive Control with Continuous-Time Constraint Satisfaction](https://arxiv.org/abs/2405.00061)

这些文献可以支持有限步 CLF 和数值求解方法的背景，但不能直接替代本文所需的时域延拓、固定前缀收敛和无限时域局部最优证明。
