# horizon continuation iLQR

## 1. 原问题

研究**对控制仿射的一类非线性系统的无穷时间最优控制问题**
$$
    \min_{\mathcal U}\quad J(x_0,\mathcal U)=\sum_{k=0}^\infty x_k^\top Qx_k+u_k^\top Ru_k=\sum_{k=0}^\infty \ell(x_k,u_k),\\
    s.t.\quad x_{k+1}=a(x_k)+B(x_k)u_k.
$$
其中$x_0\in\Omega,\mathcal U=\{u_0,u_1,\dots\},x_k\in \mathbb R^{n},u_k\in \mathbb R^m,Q\succ0,R\succ0，a(0)=0$，并且对每一个$x_k$，$a(x_k)\in\mathbb R^{n\times m},B(x_k)\in\mathbb R^{n\times m}$成立。

**求解思路**：优化一条长度递增的轨迹

## 2. 对尾部无限长代价的局部终端近似
在$x_N$足够小时，在数值上近似看作值函数。

在$(x,u)=(\mathbf0,\mathbf0)$原点处线性化
$$
    A_0=\left.\frac{\partial(a(x)+B(x)u)}{\partial x}\right|_{x=0,u=0}=a_x(0),
    \quad B_0=\left.\frac{\partial(a(x)+B(x)u)}{\partial u}\right|_{x=0,u=0}=B(0),\\\quad\\
    \bar x_{k+1}=A_0\bar x_k+B_0\bar u_k.
$$
**假设1**： 存在紧集$\Omega\subset R$使得算法访问的精确rollout留在$\Omega$的某个紧子集内，$a(\cdot)$在$\Omega$内一阶连续可微，它们的一阶导数在$\Omega$上局部Lipschitz，$(A_0,B_0)$可稳定。

基于假设1，离散代数Riccati方程
$$
    P=Q+A_0^\top P A_0-A_0^\top P B_0(R+B_0^\top P B_0)^{-1}B_0^\top P A_0
$$
有唯一正定解$P\succ0$。定义
$$
    K_f:=(R+B_0^\top P B_0)^{-1}B_0^\top P A_0,\\
    A_c:=A_0-B_0K_f.
$$
则
$$
    A_c^\top P A_c-P=-(Q+K_f^\top RK_f):=-S\prec 0.
$$
该式仅在原点处完全成立，因此对终端函数进行一定的缩放。取任意
$\gamma>1$，定义
$$
    W_f(x)=\gamma x^\top Px,\quad \kappa_f(x)=-K_fx.
$$
对应的终端集合为
$$
    \mathcal{X}_f=\{x\in\mathbb{R}^n:W_f(x)\leq\alpha_f\}.
$$

>**$\gamma>1$的作用**：
采用当前终端代价函数与反馈控制后，终端闭环映射为
$$
    F_f(x)=a(x)+B(x)\kappa_f(x)=a(x)-B(x)K_fx\\
    =a(0)+a_x(0)x+O(\|x\|^2)-B(0)K_fx+(B(x)-B(0))K_fx,\\
    (B(x)-B(0))K_fx=O(\|x\|^2).
$$
由一阶光滑性和$a(0)=0$，在足够小球内可写成
$$
    F_f(x)=A_cx+r(x),
$$
并存在$c_r>0$使得
$$
    \|r(x)\|\leq c_r\|x\|^2.
$$
终端Lyapunov函数的变化率为
$$
    W_f(F_f(x))-W_f(x)=\gamma (A_cx+r(x))^\top P(A_cx+r(x))-\gamma x^\top Px\\
    =\gamma x^\top(A_c^\top PA_c-P)x+2\gamma r(x)^\top PA_cx+\gamma r(x)^\top Pr(x)\\
    =-\gamma x^\top Sx+2\gamma r(x)^\top PA_cx+\gamma r(x)^\top Pr(x).
$$
同时，
$$
    \ell(x,\kappa_f(x))=x^\top Qx+u^\top Ru\\
    =x^\top Qx+x^\top K_f^\top RK_fx\\
    =x^\top Sx.
$$
则
$$
    W_f(F_f(x))-W_f(x)+\ell(x,\kappa_f(x))\\
    =-\gamma x^\top Sx+2\gamma r(x)^\top PA_cx+\gamma r(x)^\top Pr(x)+x^\top Sx\\
    =-(\gamma-1)x^\top Sx+2\gamma r(x)^\top PA_cx+\gamma r(x)^\top Pr(x).
$$
令
$$
    s_{min}=\lambda_{min}(S)>0, \quad p_{max}=\|P\|_2>0, \quad c_A=\|A_c^\top P\|_2>0,
$$
由Cauchy-Schwarz不等式（$a^\top b\le\|a\|_2\|b\|_2$）和余项界，得到
$$
    2\gamma r(x)^\top PA_cx\le2\gamma\|r(x)\|_2\|PA_cx\|_2\le 2\gamma c_rc_A\|x\|^3,\\
    \gamma r(x)^\top Pr(x)\le\gamma\|r(x)\|_2\|Pr(x)\|_2\le \gamma c_r^2p_{max}\|x\|^4.
$$
故
$$
    W_f(F_f(x))-W_f(x)+\ell(x,\kappa_f(x))\\
    \le-\left[(\gamma-1)s_{min}-2\gamma c_rc_A\|x\|-\gamma c_r^2p_{max}\|x\|^2\right]\|x\|^2.
$$
对于$-\gamma c_r^2p_{max}\|x\|^2-2\gamma c_rc_A\|x\|+(\gamma-1)s_{min}$，其对称轴为
$$
    \|x\|=\frac{-2\gamma c_rc_A}{2\gamma c_r^2p_{max}}=-\frac{c_A}{c_rp_{max}}<0.
$$
且$\|x\|=0$时，该表达式为$(\gamma-1)s_{min}>0$，因此存在$\rho_f>0$，使得当$\|x\|\le\rho_f$时，
$$
    W_f(F_f(x))-W_f(x)+\ell(x,\kappa_f(x))\le0,\\
    W_f(F_f(x))-W_f(x)\le-\ell(x,\kappa_f(x)).
$$
记
$$
    p_{min}=\lambda_{min}(P)>0.
$$
若$x\in\mathcal{X}_f$，则
$$
    \|x\|^2\le\frac{W_f(x)}{\gamma p_{min}}\le\frac{\alpha_f}{\gamma p_{min}}.
$$
因此，只需取
$$
    \alpha_f\le\gamma p_{min}\rho_f^2,
$$
即可保证终端约束集$\mathcal{X}_f$内的状态满足$\|x\|\le\rho_f$，从而保证终端Lyapunov函数的变化率为负。
再以上设置下，若$x\in\mathcal{X}_f$，则可以使用$\kappa_f(x),W_f(x)$作为终端控制律和终端代价函数，近似替代原问题的尾部无限长代价函数，且与最优值函数的近似误差可量化，具体地，
$$
    |W_f(x)-V^*(x)|=W_f(x)-V^*(x)\le W_f(x)\le \gamma p_{max}\|x\|^2\le \gamma p_{max}\rho_f^2.
$$
于是本算法将原问题的子问题构造分为两个阶段，当迭代轨迹的终端状态$x_N$落在终端约束集$\mathcal{X}_f$内时，使用$\kappa_f(x)$对轨迹进行时域延拓，并使用$W_f(x)$作为终端代价函数，进行有限时域的轨迹优化（第二阶段）；当$x_N$不在终端约束集$\mathcal{X}_f$内时，基于一定的规则进行轨迹的时域延拓与优化，使其逐渐收敛到终端约束集$\mathcal{X}_f$内（第一阶段）。

## 3. 第一阶段：可行性优先的轨迹短块扩展

将当前的轨迹表示为$$
    \bar{\mathcal{X}}=\{\bar{x}_0,\bar{x}_1,\dots,\bar{x}_N\},\quad
    \bar{\mathcal{U}}=\{\bar{u}_0,\bar{u}_1,\dots,\bar{u}_{N-1}\},
$$
且终端状态$\bar{x}_N\notin\mathcal{X}_f$，则第一阶段的目标是通过轨迹延拓，使得终端状态$\bar{x}_N$逐渐收敛到终端约束集$\mathcal{X}_f$内。本算法通过构造求解一系列子问题来将终端状态逐渐收束到更小的终端约束集，最终收敛到$\mathcal{X}_f$。

第一阶段算法结束后，得到的是一条“有限真实前缀+稳定反馈尾部”的可执行无限策略，不保证任何的最优性与轨迹光滑性。

具体地，将当前终点记为$z_0^l=\bar x_N$，固定终端收缩系数$0<\bar\rho<1$、有限步候选$h=1,\dots,h_{max}$和允许的中间增长系数$c\ge1$，寻找真实的控制块$U_h^l=(w_0^l,w_1^l,\dots,w_{h-1}^l),w_t\in\mathbb R^m$使
$$
    z_{t+1}^l=a(z_t^l)+B(z_t^l)w_t^l,\quad t=0,\dots,h-1,\\
    W_f(z_h^l)\le\bar\rho W_f(z_0^l),\quad W_f(z_t^l)\le cW_f(z_0^l),\quad 1\le t<h,
$$
其中$l$表示第$l$次短块扩展，$z_t^l$表示第$l$次短块扩展的第$t$个状态。若找到满足上述约束的控制块$U_h^l$，则将其作为当前轨迹的延拓，并将终端状态更新为$z_0^{l+1}=z_h^l$，继续进行下一次短块扩展。

于是子问题的问题形式为
$$
\begin{aligned}
    \inf_{U_h^l,s}\quad s\\
    s.t.\quad &z_0^l=\bar x_N,\\
    &z_{t+1}^l=a(z_t^l)+B(z_t^l)w_t^l,\quad t=0,\dots,h-1,\\
    &W_f(z_h^l) - \bar\rho W_f(z_0^l)\le s,\\
    &W_f(z_t^l) - cW_f(z_0^l)\le s,\quad 1\le t<h,\\
    &s\ge0.
\end{aligned}
$$
也可以各自设置松弛，在最小化最大违反量
$$
    \Phi_h(U_h^l)=\max\{W_f(z_h^l)-\bar\rho W_f(z_0^l),\max_{1\le t<h}\{W_f(z_t^l)-cW_f(z_0^l)\},0\}.
$$

$h=1$时，最小终端能量值为
$$
    \phi_1^{l,min}=\min_{w_0^l}\Phi_1(w_0^l)=\min_{w_0^l}W_f(a(z_0^l)+B(z_0^l)w_0^l)\\
    =\min_{w_0^l}\gamma(a(z_0^l)+B(z_0^l)w_0^l)^\top P(a(z_0^l)+B(z_0^l)w_0^l\\
    =\min_{w_0^l}\gamma(w_0^l)^\top B(z_0^l)^\top PB(z_0^l)w_0^l+2\gamma a(z_0^l)^\top PB(z_0^l)w_0^l+\gamma a(z_0^l)^\top Pa(z_0^l).
$$
令
$$
    C=P^{1/2}B(z_0^l),\quad d=P^{1/2}a(z_0^l),
$$
则该问题的最优解（$B(z_0^l)^\top P B(z_0^l)$满秩）或最小范数解（$B(z_0^l)^\top P B(z_0^l)$不满秩）为
$$
    w_1^{l,*}=-C^\dagger d,
$$
$C^\dagger$表示$C$的Moore-Penrose广义逆。该问题的最小终端能量值为
$$
    \phi_1^{l,min}=\gamma\|d-Cw_1^{l,*}\|_2^2=\gamma\|(I-CC^\dagger)d\|_2^2，
$$
判断$\phi_1^{l,min}$与$\bar\rho W_f(z_0^l)$的大小即可。

$h>1$时，随机初始化$U_h^l$，并使用多重打靶法进行轨迹优化。



**假设2**：对任意$z\in\Omega/\mathcal{X}_f$，存在一个整数$h(z)\in\{1,2,\dots,h_{max}\}$和一个真实控制块$U_{h(z)}=(w_0,w_1,\dots,w_{h(z)}-1),w_t\in\mathbb R^m$，使得从$z_0=z$出发、由原始非线性动力学精确rollout得到的轨迹$z_{t+1}=a(z_t)+B(z_t)w_t,t=0,\dots,h(z)-1$满足以下两条条件：
1. 终端状态收缩条件：
$$
    W_f(z_{h(z)})\le\bar\rho W_f(z_0),\quad 0<\bar\rho<1.
$$
2. 中间状态增长条件：
$$
    W_f(z_t)\le cW_f(z_0),\quad 1\le t<h(z),c\ge1.
$$

